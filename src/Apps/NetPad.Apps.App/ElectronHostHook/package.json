{
    "name": "netpad-electron-host-hook",
    "version": "1.0.0",
    "description": "Connector for NetPad Electron app; hook into Electron's main process.",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "electron": "^36.2.0",
        "socket.io": "^4.8.0",
        "typescript": "^5.1.6"
    }
}
