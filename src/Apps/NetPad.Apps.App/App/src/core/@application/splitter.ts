/**
 * The Split.js `gutterSize` every splitter in the app uses.
 *
 * The gutter is not a trench between surfaces — it *is* the hairline seam they meet at, so it is
 * one pixel wide. `styles/vendors/_splitjs.scss` widens the grab target with a pseudo-element and
 * assumes this value; change the two together.
 */
export const splitterGutterSize = 1;
