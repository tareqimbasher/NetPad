namespace NetPad.Common;

/// <summary>
/// These are globals we're using in a few spots, but they are bad
/// TODO find a cleaner way to get the info we need instead of using the consts defined here
/// </summary>
public static class BadGlobals
{
    public const string TargetFramework = "net6.0";
    public const int DotNetVersion = 6;
    public const string EntityFrameworkLibVersion = "6.0.7";
}
